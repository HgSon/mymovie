import React from "react";
import ReactDOM from "react-dom";
import App from "Components/App";
//.env가 자동으로 src 보게해주기때문에 "./Components/App"안해도됨

ReactDOM.render(<App />, document.getElementById("root"));
